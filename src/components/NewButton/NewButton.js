import React from 'react';
import Button from '../Button/Button';
import AddIcon from '@material-ui/icons/Add';

const NewButton = () => <Button text="New Note" Icon={AddIcon} />;

export default NewButton;
