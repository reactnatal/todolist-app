import React from 'react';
import Button from '../Button/Button';
import SaveIcon from '@material-ui/icons/Save';

const SaveButton = () => <Button text="Save" Icon={SaveIcon} />;

export default SaveButton;
